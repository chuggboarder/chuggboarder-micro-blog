# Ignore raw data files. "Raw data" in this
# context is a time sensitive, externally reproducable slice, the transient
# component of a "report". The date it was pulled is appended to the filename.
*_202?????.*

# Ignore emacs temp files ('~' or '#' in the filename)
*#*
*~*

# Ignore the manually coded subqueries made for test purposes
*-manual.*

# Ignore DB files.  These sql queries are designed to produce the DB from scratch each time its run
*.db